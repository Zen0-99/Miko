feat: history batching, novel stats, updates tab reorder, extension update-all

History:
- Record history entries when chapters/episodes are manually marked as
  read/watched (SetReadStatus, SetNovelReadStatus, SetSeenStatus now upsert
  history with readAt=now, duration=0)
- Batch close history events: 5+ chapters/episodes from the same title
  marked within 1 minute collapse into a single row showing
  "Chapter X - Y" / "Episode X - Y" with time range "X AM - Y AM"
  (applied to novel, manga, and anime history)

Stats:
- Add NovelStatsScreenModel so novel stats actually compute from the
  novel library instead of reusing the (empty) manga stats

Updates tab:
- Reorder sub-tabs to Updates, Calendar, Downloads, Stats, History
- Remove the "Update library" (Refresh) icon from the updates top bar
  across novel/anime/manga

Extensions:
- Add a download icon inline with the "Installed" header that triggers
  "update all installed extensions with pending updates" (only updates
  installed extensions, does not install uninstalled ones)
