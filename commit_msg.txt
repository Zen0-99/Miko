fix: extension card redesign, history visibility, tracking pill, interval badge, default repo seed

Extension card (sources browse):
- Redesign layout: icon top-left, cog top-right, lang/version above title
  bottom-left, comments badge bottom-right, with visual gap between rows
- Use surfaceContainerHigh for a lighter-than-background card fill
- Fix 2-column layout: replace FlowRow weight() (which collapsed cards
  into 1 column) with fillMaxWidth(fraction) on the card directly
  (applied to novel, manga, anime sources screens)

Chapter long-press sheet:
- Replace generic DoneAll/Undo/SelectAll/RemoveDone icons with custom
  eye-based vectors matching Miko-Yokai-Old:
  - EyeDown (mark previous as read)
  - EyeOffDown (mark previous as unread)
  - EyeDots (mark range as read)
  - EyeOffDots (mark range as unread)

Novel tracking pill:
- Always show the tracking pill in the action row, not only when the
  user has logged-in trackers configured

Interval badge:
- Always show the interval badge inline with the chapter/episode count
  on all detail views; display "N/A" when the interval can't be
  calculated instead of hiding the badge

History visibility:
- Remove the maxReadAtChapterId/maxSeenAtEpisodeId filter from the
  history view queries so all mass-marked chapters/episodes appear in
  history, not just the most recent one per title
  (novel, manga, anime history views)

Extension update detection:
- Seed the default MikoNovelSources extension repo on first launch so
  available extensions and version updates are detected without manual
  repo setup in Settings
